#include <stdio.h>
/**
 * main- Entry point
 * @argc: argument count with int type
 * @argv: argument vector with char pointer
 *
 * Return: 0 Always (Success)
 */
int main(int argc, char *argv[])
{
	(void)argc;
	printf("%s\n", argv[0]);
	return (0);
}
